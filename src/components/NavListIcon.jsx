import React from "react"

export default class NavListIcon extends React.Component {
    render(){
        return(
            <a href="#" style={{height: "100%"}}>
                <div>
                    
                </div>
            </a>
        )
    }
}